# Backend

Node.js and Express application code. Run it from the repository root with
`npm start`.

The active SQLite database, its access module, and SQL schemas are organised
under `backend/database/`.
