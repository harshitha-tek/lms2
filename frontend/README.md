# Frontend

Server-rendered EJS views and browser assets for the LMS.

- `views/` contains pages and reusable partials.
- `public/` contains CSS, client JavaScript, and uploaded profile images.

Express serves these files through `backend/app.js`.
